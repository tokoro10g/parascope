name: "Vector Magnitude 2D"
nodes:
  x: { type: input, label: "X", value: 3 }
  y: { type: input, label: "Y", value: 4 }
  calc:
    type: function
    code: "magnitude = math.sqrt(x**2 + y**2)"
    inputs: { x: x, y: y }
  out: { type: output, label: "Magnitude", input: calc.magnitude }
