name: "Angle from XY"
nodes:
  x: { type: input, label: "X", value: 1 }
  y: { type: input, label: "Y", value: 1 }
  calc:
    type: function
    code: "angle_deg = math.degrees(math.atan2(y, x))"
    inputs: { x: x, y: y }
  out: { type: output, label: "Angle [deg]", input: calc.angle_deg }
